angular.module('myApp', ['ngRoute'])

angular.module('myApp').config(function ($routeManager) {
  //DEFINE ROUTES HERE

  $routeManager
    .when('', {
      template: ''
    })
    .when('', {
      template: ''
    });
});